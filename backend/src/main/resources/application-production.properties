# Production overrides. Secrets remain in the hosting provider's environment.
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=none

authorization-server.issuer=${AUTH_SERVER_ISSUER:${AUTHORIZATION_SERVER_ISSUER:https://rental-system-1-72fo.onrender.com}}
authorization-server.client.redirect-uris=${RENTAL_SPA_REDIRECT_URIS:https://rental-system-bice.vercel.app/callback}

security.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://rental-system-bice.vercel.app}
app.frontend.base-url=${FRONTEND_URL:https://rental-system-bice.vercel.app}
