# Local MySQL. Fall back to existing DB_* variables for local/Docker setups.
spring.datasource.url=${LOCAL_DB_URL:${DB_URL}}
spring.datasource.username=${LOCAL_DB_USERNAME:${DB_USERNAME}}
spring.datasource.password=${LOCAL_DB_PASSWORD:${DB_PASSWORD}}
